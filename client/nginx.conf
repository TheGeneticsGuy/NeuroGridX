server {
    listen 80;

    # The root directory where the built React app lives
    root /usr/share/nginx/html;
    index index.html;

    # Important: This block handles React Router
    # If a file isn't found (like /dashboard), serve index.html instead
    location / {
        try_files $uri $uri/ /index.html;
    }
}